#python 3.5.2
# 숫자 엔개 중에서 최댓값 찾기를 재귀 호출

def get_max_value(a):
    max = a[0]; 
    for i in range(len(a)):
        if(max < a[i]):
            max = a[i]
    
    return max
    #print(max)
       
        
    

list = [ 1,3,5,2, 4];
print(get_max_value(list))
